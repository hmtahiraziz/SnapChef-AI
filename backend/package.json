{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for ingredient extraction, recipe generation, and favorites persistence.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start:venv:win": ".\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start:venv:unix": "./.venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "install:deps": "python -m pip install -r requirements.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
