/**
 * Resolves `/settings` alongside the Settings tab without stealing the screen.
 * Nested prefs live under `/preferences` — see cuisine/appearance redirects.
 */
export { default } from '../(tabs)/settings';
